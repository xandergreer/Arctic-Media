# Secrets — never commit these
.env
.secret_key

# automatic python environment
venv/

# large executables that violate GitHub's 100MB push limit
bin/

# temporary compiled cache
__pycache__/
*.pyc

# temporary logs
*.log
backend_debug.txt
debug_clover.txt

# exclude large executables to avoid pushing failures
*.exe
*.pkg
